pipelines:
  general:
    debug: true
  tools:
    branches:
      patterns:
        feature: .+
  branches:
    feature:
      steps:
        - test:
          - work:
              name: first
          - work:
              name: second
          - work:
              name: third
