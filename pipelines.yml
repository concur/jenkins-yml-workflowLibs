pipelines:
  general:
    debug: true
  tools:
    branches:
      patterns:
        feature: .+
    slack:
      channel: 'git-notifications'
  branches:
    feature:
      steps:
        - github:
          - createRelease:
