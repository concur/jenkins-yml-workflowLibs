pipelines:
  general:
    debug: true
  tools:
    branches:
      patterns:
        feature: .+
    github:
      credentials:
        description: GitHub Secret Text Access Token
    jenkins:
      workflows:
        branch: github-release
    slack:
      channel: 'git-notifications'
  branches:
    feature:
      steps:
        - github:
          - createRelease:
