pipelines:
  general:
    debug: true
  tools:
    branches:
      patterns:
        feature: .+
    slack:
      channel: 'git-notifications'
  branches:
    feature:
      steps:
        - example:
          - work:
              name: first
          - work:
              name: second
          - work:
              name: third
